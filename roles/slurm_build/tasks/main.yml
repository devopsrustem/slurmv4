# roles/slurm_build/tasks/main.yml
---
# Роль: slurm_build - компиляция Slurm 25.05.1 с JWT поддержкой
# Ubuntu 24.04 + ИСПРАВЛЕННОЕ РАСПРОСТРАНЕНИЕ HEADERS

- name: "[SLURM_BUILD] Начало сборки Slurm"
  debug:
    msg: "🔧 Сборка Slurm {{ slurm_version }} с JWT поддержкой на {{ inventory_hostname }}"

- name: "[SLURM_BUILD] Очистка старой сборки"
  shell: rm -rf {{ slurm_build_dir }}
  when: inventory_hostname in groups['slurm_master']

# =============================================================================
# ПОДГОТОВКА К СБОРКЕ (ТОЛЬКО НА MASTER)
# =============================================================================

- name: "[SLURM_BUILD] Установка дополнительных зависимостей для сборки"
  apt:
    name: "{{ slurm_build_dependencies }}"
    state: present
  when: inventory_hostname in groups['slurm_master']
  tags:
    - dependencies

- name: "[SLURM_BUILD] Создание временной директории для сборки"
  file:
    path: "{{ slurm_build_dir }}"
    state: directory
    owner: root
    group: root
    mode: '0755'
  when: inventory_hostname in groups['slurm_master']
  tags:
    - download
    - build

- name: "[SLURM_BUILD] Проверка существования исходников"
  stat:
    path: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}.tar.bz2"
  register: slurm_tarball
  when: inventory_hostname in groups['slurm_master']
  tags:
    - download

- name: "[SLURM_BUILD] Скачивание исходников Slurm {{ slurm_version }}"
  get_url:
    url: "{{ slurm_download_url }}"
    dest: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}.tar.bz2"
    mode: '0644'
    timeout: 300
  when: 
    - inventory_hostname in groups['slurm_master']
    - not slurm_tarball.stat.exists
  tags:
    - download

- name: "[SLURM_BUILD] Извлечение исходников Slurm"
  unarchive:
    src: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}.tar.bz2"
    dest: "{{ slurm_build_dir }}"
    remote_src: yes
    creates: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}"
  when: inventory_hostname in groups['slurm_master']
  tags:
    - build

# =============================================================================
# КОНФИГУРАЦИЯ И СБОРКА
# =============================================================================

- name: "[SLURM_BUILD] Конфигурация сборки Slurm с JWT поддержкой"
  shell: |
    cd {{ slurm_build_dir }}/slurm-{{ slurm_version }}
    ./configure \
      --prefix={{ slurm_install_prefix }} \
      --sysconfdir=/etc/slurm \
      --enable-mysql \
      --with-mysql_config=/usr/bin \
      --with-munge \
      --with-hwloc \
      --with-json \
      --with-ssl \
      --with-jwt \
      --enable-multiple-scontrollers \
      --enable-pam \
      --with-pam_dir=/lib/x86_64-linux-gnu/security/ \
      --with-pmix \
      --with-systemd \
      --enable-slurmrestd \
      --enable-debug \
      --enable-developer
  args:
    chdir: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}"
    creates: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}/config.log"
  when: inventory_hostname in groups['slurm_master']
  register: slurm_configure
  tags:
    - build

- name: "[SLURM_BUILD] Компиляция Slurm (это займет время...)"
  shell: |
    cd {{ slurm_build_dir }}/slurm-{{ slurm_version }}
    make -j$(nproc)
  args:
    chdir: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}"
    creates: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}/src/slurmctld/slurmctld"
  when: inventory_hostname in groups['slurm_master']
  register: slurm_compile
  tags:
    - build

- name: "[SLURM_BUILD] Установка Slurm на master"
  shell: |
    cd {{ slurm_build_dir }}/slurm-{{ slurm_version }}
    make install
  args:
    chdir: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}"
    creates: "{{ slurm_install_prefix }}/sbin/slurmctld"
  when: inventory_hostname in groups['slurm_master']
  register: slurm_install
  tags:
    - build

- name: "[SLURM_BUILD] Установка development файлов (headers для Pyxis)"
  shell: |
    cd {{ slurm_build_dir }}/slurm-{{ slurm_version }}
    # Копируем headers в правильное место
    mkdir -p {{ slurm_install_prefix }}/include/slurm
    cp -r slurm/*.h {{ slurm_install_prefix }}/include/slurm/
    # Копируем главный header из src
    cp src/slurmd/slurmd.h {{ slurm_install_prefix }}/include/slurm/ 2>/dev/null || true
    echo "Headers установлены в {{ slurm_install_prefix }}/include/slurm/"
  args:
    chdir: "{{ slurm_build_dir }}/slurm-{{ slurm_version }}"
    creates: "{{ slurm_install_prefix }}/include/slurm/spank.h"
  when: inventory_hostname in groups['slurm_master']
  register: slurm_dev_install
  tags:
    - build
    - dev

# =============================================================================
# СОЗДАНИЕ NFS АРХИВА ДЛЯ РАСПРОСТРАНЕНИЯ (ИСПРАВЛЕНО!)
# =============================================================================

- name: "[SLURM_BUILD] Создание директории Slurm на всех узлах"
  file:
    path: "{{ slurm_install_prefix }}"
    state: directory
    owner: root
    group: root
    mode: '0755'
  tags:
    - distribute

- name: "[SLURM_BUILD] Создание полного архива Slurm в NFS"
  shell: |
    set -e
    echo "=== СОЗДАНИЕ АРХИВА SLURM + HEADERS ==="
    
    # Очистка старого архива
    rm -rf /sw/slurm-complete
    mkdir -p /sw/slurm-complete
    
    # Копируем ВСЁ из /opt/slurm
    echo "Копируем binaries..."
    cp -r {{ slurm_install_prefix }}/* /sw/slurm-complete/
    
    # КРИТИЧНО: Проверяем что headers скопированы!
    if [ -f /sw/slurm-complete/include/slurm/spank.h ]; then
      echo "✅ Headers включены в архив: $(find /sw/slurm-complete/include -name '*.h' | wc -l) файлов"
    else
      echo "❌ ОШИБКА: Headers не найдены в архиве!"
      exit 1
    fi
    
    # Устанавливаем права
    chown -R root:root /sw/slurm-complete
    chmod -R 755 /sw/slurm-complete/bin /sw/slurm-complete/sbin
    chmod -R 644 /sw/slurm-complete/include
    
    echo "=== АРХИВ ГОТОВ ==="
    echo "Binaries: $(ls /sw/slurm-complete/bin | wc -l) файлов"
    echo "Daemons: $(ls /sw/slurm-complete/sbin | wc -l) файлов"
    echo "Headers: $(find /sw/slurm-complete/include -name '*.h' | wc -l) файлов"
    echo "Libs: $(ls /sw/slurm-complete/lib | wc -l) файлов"
  when: inventory_hostname in groups['slurm_master']
  register: archive_creation
  tags:
    - distribute

- name: "[SLURM_BUILD] Результат создания архива"
  debug:
    var: archive_creation.stdout_lines
  when: inventory_hostname in groups['slurm_master']
  tags:
    - distribute

- name: "[SLURM_BUILD] Ожидание готовности архива"
  wait_for:
    path: "/sw/slurm-complete/include/slurm/spank.h"
    timeout: 30
  when: inventory_hostname not in groups['slurm_master']
  tags:
    - distribute

- name: "[SLURM_BUILD] Установка Slurm на все узлы из архива"
  shell: |
    set -e
    echo "=== УСТАНОВКА SLURM ИЗ АРХИВА ==="
    
    # Останавливаем slurmd если запущен
    systemctl stop slurmd 2>/dev/null || true
    
    # Копируем всё из архива
    cp -r /sw/slurm-complete/* {{ slurm_install_prefix }}/
    
    # Устанавливаем права
    chown -R root:root {{ slurm_install_prefix }}
    chmod -R 755 {{ slurm_install_prefix }}/bin {{ slurm_install_prefix }}/sbin
    chmod -R 644 {{ slurm_install_prefix }}/include
    
    # Проверяем что headers установлены
    if [ -f {{ slurm_install_prefix }}/include/slurm/spank.h ]; then
      echo "✅ Headers установлены: $(find {{ slurm_install_prefix }}/include -name '*.h' | wc -l) файлов"
    else
      echo "❌ ОШИБКА: Headers не установлены!"
      exit 1
    fi
    
    echo "=== УСТАНОВКА ЗАВЕРШЕНА ==="
  when: inventory_hostname not in groups['slurm_master']
  register: slurm_install_nodes
  tags:
    - distribute

- name: "[SLURM_BUILD] Результат установки на узлах"
  debug:
    var: slurm_install_nodes.stdout_lines
  when: inventory_hostname not in groups['slurm_master']
  tags:
    - distribute

# =============================================================================
# НАСТРОЙКА ПУТЕЙ И СИСТЕМНЫХ ФАЙЛОВ
# =============================================================================

- name: "[SLURM_BUILD] Создание символических ссылок для пользовательских команд Slurm"
  file:
    src: "{{ slurm_install_prefix }}/bin/{{ item }}"
    dest: "/usr/bin/{{ item }}"
    state: link
    force: yes
  loop:
    - sinfo
    - squeue
    - scancel
    - sbatch
    - salloc
    - srun
    - sacct
    - scontrol
    - sprio
    - smap
    - sdiag
    - sstat
    - sreport
    - sacctmgr
    - sattach
    - sbcast
    - sshare
    - sview
  tags:
    - links

- name: "[SLURM_BUILD] Создание символических ссылок для системных демонов Slurm"
  file:
    src: "{{ slurm_install_prefix }}/sbin/{{ item }}"
    dest: "/usr/sbin/{{ item }}"
    state: link
    force: yes
  loop:
    - slurmctld
    - slurmd
    - slurmdbd
    - slurmrestd
    - slurmstepd
  tags:
    - links

- name: "[SLURM_BUILD] Обновление библиотечного кеша"
  shell: ldconfig
  tags:
    - links

# =============================================================================
# ПРОВЕРКА УСТАНОВКИ
# =============================================================================

- name: "[SLURM_BUILD] Проверка версии Slurm"
  command: "{{ slurm_install_prefix }}/bin/sinfo -V"
  register: slurm_version_check
  changed_when: false
  failed_when: false
  tags:
    - verify

- name: "[SLURM_BUILD] Проверка поддержки JWT"
  shell: "{{ slurm_install_prefix }}/sbin/slurmctld -V 2>&1 | grep -i jwt || echo 'JWT support check'"
  register: jwt_check
  changed_when: false
  failed_when: false
  tags:
    - verify

- name: "[SLURM_BUILD] Проверка headers (КРИТИЧНО ДЛЯ PYXIS!)"
  shell: |
    if [ -f {{ slurm_install_prefix }}/include/slurm/spank.h ]; then
      echo "✅ Headers найдены: $(find {{ slurm_install_prefix }}/include -name '*.h' | wc -l) файлов"
      echo "✅ spank.h: $(ls -la {{ slurm_install_prefix }}/include/slurm/spank.h)"
    else
      echo "❌ Headers НЕ НАЙДЕНЫ - Pyxis НЕ СКОМПИЛИРУЕТСЯ!"
    fi
  register: headers_check
  changed_when: false
  failed_when: false
  tags:
    - verify

- name: "[SLURM_BUILD] Результаты проверки"
  debug:
    msg: |
      ✅ Slurm успешно установлен на {{ inventory_hostname }}
      📋 Версия: {{ slurm_version_check.stdout }}
      🔐 JWT поддержка: {{ jwt_check.stdout }}
      📁 Headers: {{ headers_check.stdout }}
      📍 Путь установки: {{ slurm_install_prefix }}
      🔗 Команды доступны в /usr/bin/, демоны в /usr/sbin/
  tags:
    - verify

- name: "[SLURM_BUILD] Сборка завершена успешно"
  debug:
    msg: "🎉 Slurm {{ slurm_version }} с headers готов к установке контейнеров на {{ inventory_hostname }}"