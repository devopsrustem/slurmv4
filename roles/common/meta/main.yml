---
# Метаданные роли common

galaxy_info:
  author: DevOps Team
  description: Common role for Slurm HPC cluster - base system setup, MUNGE, NFS, HWLOC
  company: HPC Lab
  license: MIT
  min_ansible_version: 2.15
  
  platforms:
    - name: Ubuntu
      versions:
        - 24.04

  galaxy_tags:
    - hpc
    - slurm
    - cluster
    - munge
    - nfs
    - hwloc
    - system

# Зависимости роли
dependencies: []
  # Роль common не зависит от других ролей
  # Она сама является базой для всех остальных ролей

# Определяем, что эта роль должна выполняться первой
allow_duplicates: false