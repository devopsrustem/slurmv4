# roles/containers/tasks/main.yml
---
# МИНИМАЛЬНОЕ ИСПРАВЛЕНИЕ - ТОЛЬКО ПРОБЛЕМА С PYXIS + ДОБАВЛЯЕМ ENROOT+CAPS

- name: "[CONTAINERS] Исправление Pyxis + добавление enroot+caps"
  debug:
    msg: "Исправляем Pyxis + добавляем enroot+caps для compute узлов"

- name: "[CONTAINERS] Задание переменных"
  set_fact:
    pyxis_build_dir: "/tmp/pyxis-build"
    pyxis_version: "0.20.0"
    pyxis_download_url: "https://github.com/NVIDIA/pyxis/archive/v0.20.0.tar.gz"

# =============================================================================
# СИСТЕМНЫЕ ЗАВИСИМОСТИ (ДОБАВЛЯЕМ ТОЛЬКО НУЖНОЕ)
# =============================================================================

- name: "[CONTAINERS] Установка libslurm-dev (для headers)"
  apt:
    name: libslurm-dev
    state: present
    update_cache: yes

# =============================================================================
# УСТАНОВКА ENROOT (ОРИГИНАЛЬНАЯ ВЕРСИЯ)
# =============================================================================

- name: "[CONTAINERS] Проверка установки Enroot"
  command: enroot version
  register: enroot_check
  failed_when: false
  changed_when: false

- name: "[CONTAINERS] Установка Enroot"
  shell: |
    if ! command -v enroot >/dev/null 2>&1; then
      cd /tmp
      wget -q https://github.com/NVIDIA/enroot/releases/download/v3.5.0/enroot_3.5.0-1_amd64.deb
      apt install -y ./enroot_3.5.0-1_amd64.deb
      echo "Enroot установлен"
    else
      echo "Enroot уже установлен"
    fi
  register: enroot_install

# =============================================================================
# УСТАНОВКА ENROOT+CAPS (ПРИНУДИТЕЛЬНО НА COMPUTE УЗЛАХ)
# =============================================================================

- name: "[CONTAINERS] Установка enroot+caps на compute узлах (принудительно)"
  shell: |
    echo "=== УСТАНОВКА ENROOT+CAPS ==="
    cd /tmp
    
    # Скачиваем enroot+caps версию
    wget -q https://github.com/NVIDIA/enroot/releases/download/v3.5.0/enroot+caps_3.5.0-1_amd64.deb
    
    # Устанавливаем принудительно
    dpkg -i enroot+caps_3.5.0-1_amd64.deb
    
    # Проверяем capabilities
    echo "✅ Capabilities установлены:"
    getcap /usr/bin/enroot-aufs2ovlfs || echo "capabilities не найдены"
    
    echo "✅ enroot+caps установлен на {{ inventory_hostname }}"
  when: inventory_hostname in groups['slurm_compute']
  register: enroot_caps_install

- name: "[CONTAINERS] Результат установки enroot+caps"
  debug:
    var: enroot_caps_install.stdout_lines
  when: 
    - inventory_hostname in groups['slurm_compute']
    - enroot_caps_install is defined

# =============================================================================
# ИСПРАВЛЕННАЯ УСТАНОВКА PYXIS (ГЛАВНОЕ ИСПРАВЛЕНИЕ!)
# =============================================================================

- name: "[PYXIS] Очистка старых файлов"
  file:
    path: "{{ pyxis_build_dir }}"
    state: absent

- name: "[PYXIS] Создание build директории"
  file:
    path: "{{ pyxis_build_dir }}"
    state: directory
    mode: '0755'

- name: "[PYXIS] Скачивание исходников"
  get_url:
    url: "{{ pyxis_download_url }}"
    dest: "/tmp/pyxis-{{ pyxis_version }}.tar.gz"
    timeout: 300

- name: "[PYXIS] Извлечение исходников"
  unarchive:
    src: "/tmp/pyxis-{{ pyxis_version }}.tar.gz"
    dest: "{{ pyxis_build_dir }}"
    remote_src: yes
    extra_opts: ["--strip-components=1"]

- name: "[PYXIS] ИСПРАВЛЕННАЯ компиляция"
  shell: |
    cd {{ pyxis_build_dir }}
    
    echo "=== ИСПРАВЛЕННАЯ КОМПИЛЯЦИЯ PYXIS ==="
    
    # ПРОБУЕМ НЕСКОЛЬКО ПУТЕЙ К HEADERS
    if [ -f /opt/slurm/include/slurm/spank.h ]; then
      HEADER_PATH="/opt/slurm/include"
      echo "Используем ваши скомпилированные headers: $HEADER_PATH"
    elif [ -f /usr/include/slurm/spank.h ]; then
      HEADER_PATH="/usr/include"
      echo "Используем системные headers: $HEADER_PATH"
    else
      echo "❌ Headers не найдены!"
      exit 1
    fi
    
    # ПРОБУЕМ НЕСКОЛЬКО ПУТЕЙ К БИБЛИОТЕКАМ
    if [ -f /opt/slurm/lib/libslurm.so ]; then
      LIB_PATH="/opt/slurm/lib"
      echo "Используем ваши скомпилированные библиотеки: $LIB_PATH"
    elif [ -f /usr/lib/x86_64-linux-gnu/libslurm.so ]; then
      LIB_PATH="/usr/lib/x86_64-linux-gnu"
      echo "Используем системные библиотеки: $LIB_PATH"
    else
      echo "❌ Библиотеки не найдены!"
      exit 1
    fi
    
    # Компиляция с найденными путями
    export CPPFLAGS="-I${HEADER_PATH}"
    export LDFLAGS="-L${LIB_PATH}"
    
    make clean || true
    make
    
    # Установка
    make install
    
    echo "✅ Pyxis установлен!"
    ls -la /usr/local/lib/slurm/spank_pyxis.so
    
  register: pyxis_install

# =============================================================================
# КОНФИГУРАЦИЯ (ИСПРАВЛЯЕМ ТОЛЬКО ПУТЬ)
# =============================================================================

- name: "[CONFIG] Создание ПРАВИЛЬНОГО plugstack.conf"
  copy:
    dest: /etc/slurm/plugstack.conf
    content: |
      # Slurm SPANK Plugin Stack Configuration
      required /usr/local/lib/slurm/spank_pyxis.so
    owner: root
    group: root
    mode: '0644'

# =============================================================================
# ДИРЕКТОРИИ ENROOT (БЕЗ ИЗМЕНЕНИЙ)
# =============================================================================

- name: "[ENROOT] Создание директорий Enroot"
  file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - /var/lib/enroot
    - /var/cache/enroot
    - /run/enroot
    - /etc/enroot

# =============================================================================
# СОЗДАНИЕ ДИРЕКТОРИЙ ENROOT С ПРАВИЛЬНЫМИ ПРАВАМИ
# =============================================================================

- name: "[ENROOT] Создание директорий Enroot с правильными правами"
  shell: |
    echo "=== СОЗДАНИЕ ENROOT ДИРЕКТОРИЙ ==="
    
    # Создаем все директории
    mkdir -p /var/lib/enroot-cache /run/enroot /tmp/enroot-data /etc/enroot
    
    # Снимаем immutable атрибуты
    chattr -i /var/lib/enroot-cache /run/enroot /tmp/enroot-data 2>/dev/null || true
    
    # Устанавливаем права 1777
    chmod 1777 /var/lib/enroot-cache /run/enroot /tmp/enroot-data
    
    # Проверяем
    ls -ld /var/lib/enroot-cache /run/enroot /tmp/enroot-data
    
  register: enroot_dirs_setup

# =============================================================================
# ПРОДАКШН КОНФИГУРАЦИЯ ENROOT (НОВОЕ!)
# =============================================================================

- name: "[ENROOT] Продакшн конфигурация Enroot"
  template:
    src: enroot.conf.j2
    dest: /etc/enroot/enroot.conf
    owner: root
    group: root
    mode: '0644'
  tags: config

# =============================================================================
# МИНИМАЛЬНАЯ ПРОВЕРКА
# =============================================================================

- name: "[VERIFY] Проверка исправлений"
  shell: |
    echo "=== ПРОВЕРКА ИСПРАВЛЕНИЙ ==="
    
    if [ -f /usr/local/lib/slurm/spank_pyxis.so ]; then
      echo "✅ Pyxis plugin: $(ls -la /usr/local/lib/slurm/spank_pyxis.so)"
    else
      echo "❌ Pyxis plugin НЕ НАЙДЕН"
    fi
    
    if [ -f /etc/slurm/plugstack.conf ]; then
      echo "✅ plugstack.conf:"
      cat /etc/slurm/plugstack.conf
    else
      echo "❌ plugstack.conf НЕ НАЙДЕН"
    fi
    
    {% if inventory_hostname in groups['slurm_compute'] %}
    echo "✅ Enroot+caps check:"
    getcap /usr/bin/enroot-aufs2ovlfs 2>/dev/null || echo "capabilities не найдены"
    {% endif %}
    
  register: fix_verify
  changed_when: false

- name: "[CONTAINERS] Результат исправления"
  debug:
    msg: |
      🔧 Исправления завершены:
      {{ fix_verify.stdout }}
      
      {% if inventory_hostname in groups['slurm_compute'] %}
      🎯 На compute узле также установлен enroot+caps
      {% endif %}
      
      Теперь перезапустите slurmd:
      sudo systemctl restart slurmd