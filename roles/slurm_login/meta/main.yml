# roles/login_nodes/meta/main.yml
---
# Метаданные роли login_nodes

galaxy_info:
  author: DevOps Team
  description: Slurm login node role - user access point to HPC cluster
  company: HPC Lab
  license: MIT
  min_ansible_version: 2.15
  
  platforms:
    - name: Ubuntu
      versions:
        - 24.04

  galaxy_tags:
    - hpc
    - slurm
    - cluster
    - login
    - users
    - ssh

# Зависимости роли
dependencies:
  - role: common
    # Базовая настройка узла - MUNGE, NFS, пользователи

# Login узел НЕ требует slurm_build - только клиентские команды через NFS
allow_duplicates: false