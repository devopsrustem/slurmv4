# site.yml - Общий playbook установки кластера
---
- name: "🚀 УСТАНОВКА SLURM HPC КЛАСТЕРА"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Информация о развертывании"
      debug:
        msg: |
          🚀 Установка Slurm HPC кластера
          📍 Master:  {{ groups['slurm_master'] | join(', ') }}
          📍 Login:   {{ groups['slurm_login'] | join(', ') }}
          📍 Compute: {{ groups['slurm_compute'] | join(', ') }}

# ЭТАП 1: Базовая подготовка
- name: "ЭТАП 1: Базовая подготовка"
  hosts: slurm_cluster
  become: true
  gather_facts: true
  roles:
    - common

# ЭТАП 2: Компиляция и распространение Slurm 
- name: "ЭТАП 2: Компиляция и распространение Slurm"
  hosts: slurm_cluster
  become: true
  gather_facts: true
  roles:
    - slurm_build

# ЭТАП 3: Настройка master
- name: "ЭТАП 3: Настройка master"
  hosts: slurm_master
  become: true
  gather_facts: true
  roles:
    - slurm_master

# ЭТАП 4: Настройка compute узлов
- name: "ЭТАП 4: Настройка compute"
  hosts: slurm_compute
  become: true
  gather_facts: true
  roles:
    - slurm_compute

# ЭТАП 5: Настройка login узлов
- name: "ЭТАП 5: Настройка login"
  hosts: slurm_login
  become: true
  gather_facts: true
  roles:
    - slurm_login

# ЭТАП 6: Отключение AppArmor на compute узлах
- name: "ЭТАП 6: Отключение AppArmor"
  hosts: slurm_compute
  become: true
  gather_facts: true
  roles:
    - grub_apparmor

# ЭТАП 7: Установка Enroot через nvidia.enroot роль
- name: "ЭТАП 7: Установка Enroot"
  hosts: slurm_cluster
  become: true
  gather_facts: true
  roles:
    - nvidia.enroot

# ЭТАП 8: Установка Pyxis для контейнерной интеграции
- name: "ЭТАП 8: Установка Pyxis"
  hosts: slurm_cluster
  become: true
  gather_facts: true
  roles:
    - pyxis

# ЭТАП 9: Перезапуск сервисов
- name: "ЭТАП 8: Перезапуск сервисов"
  hosts: slurm_cluster
  become: true
  tasks:
    - name: "Перезапуск slurmd"
      systemd:
        name: slurmd
        state: restarted
        daemon_reload: yes
      when: inventory_hostname in groups['slurm_compute']

    - name: "Перезапуск slurmctld"
      systemd:
        name: slurmctld
        state: restarted
        daemon_reload: yes
      when: inventory_hostname in groups['slurm_master']